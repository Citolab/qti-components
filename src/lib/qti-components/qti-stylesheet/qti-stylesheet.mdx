import { Canvas, Meta, Story } from '@storybook/blocks';

import * as Stories from './qti-stylesheet.stories';

<Meta of={Stories} tags={['hidden-docs']} />

# stylesheet

A stylesheet is a square box that can be activated or deactivated when ticked.

Use stylesheets to select one or more options from a list of choices.

## Linked variant

<Canvas of={Stories.Default} />

## Inline variant ( not qti supported )

<Canvas of={Stories.Inline} />
