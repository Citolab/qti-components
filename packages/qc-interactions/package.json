{
    "name": "@qti-components/interactions",
    "version": "1.0.0",
    "description": "QTI interactions utilities",
    "main": "dist/index.js",
    "scripts": {
        "build": "wireit",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "keywords": [
        "qti",
        "interactions"
    ],
    "author": "",
    "license": "GPL-3.0-only",
    "dependencies": {
        "@qti-components/shared": "^1.0.0",
        "@qti-components/transformers": "^1.0.0",
        "@qti-components/utilities": "^1.0.0"
    },
    "peerDependencies": {
        "lit": "^3.2.1"
    },
    "wireit": {
        "build": {
            "command": "tsc --pretty",
            "files": [
                "src/**/*.ts",
                "!src/**/*.test.ts",
                "!src/**/*.spec.ts",
                "!src/**/*.stories.ts",
                "tsconfig.json"
            ],
            "output": [
                "dist/*.{d.ts,d.ts.map,js,js.map}"
            ],
            "clean": "if-file-deleted"
        }
    }
}